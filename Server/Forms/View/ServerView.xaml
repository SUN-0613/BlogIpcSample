<Window x:Class="Server.Forms.View.ServerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Server.Forms.ViewModel"
        mc:Ignorable="d"
        Title="ServerView" Height="150" Width="300">
    <Window.DataContext>
        <vm:ServerViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Now = " VerticalAlignment="Center" />
            <TextBlock Text="{Binding PresentValue}" VerticalAlignment="Center" />
        </StackPanel>
        <Button Grid.Row="1" Content="切断" IsEnabled="{Binding IsEnabled}">
            <Button.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding CloseCommand}" />
            </Button.InputBindings>
        </Button>
    </Grid>
</Window>
